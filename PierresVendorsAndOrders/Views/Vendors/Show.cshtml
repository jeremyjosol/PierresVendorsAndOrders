@{
  Layout = "_Layout";
}

@using PierresVendorsAndOrders.Models;

<div class="links">
  
  <h2>Orders:</h2>

  @if (!((List<Order>)Model["orders"]).Any())
  {
    <p>Your order list is empty.</p>
  }
    
  <ol>
  @foreach (Order order in @Model["orders"])
  {
    <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.Id. @order.Product</a></li>
  }
  </ol>
  <a href='/vendors/@Model["vendor"].Id/orders/new'>Add a new product |</a>
  <a href='/vendors'>Return to vendor list |</a>
 <a href='/'>Return to Main Page</a>
</div>