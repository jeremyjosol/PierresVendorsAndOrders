@{
  Layout = "_Layout";
}

@using PierresVendorsAndOrders.Models;

<div class="links">
  
  <h1>@Model["vendor"].Name 
  <br />  
  Order List:</h1>
  <br />

  @if (!((List<Order>)Model["orders"]).Any())
  {
    <p>Your order list is empty.</p>
  }
    
  <ol>
  @foreach (Order order in @Model["orders"])
  {
    <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.Product | ID #@order.Id</a></li>
  }
  </ol>
  <a href='/vendors/@Model["vendor"].Id/orders/new'>New Order |</a>
  <a href='/vendors'>View Vendors |</a>
 <a href='/'>Home</a>
</div>